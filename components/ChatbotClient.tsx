// app/components/ChatbotClient.tsx
"use client";

import React from "react";
import Chatbot from "@/components/Chatbot"; // Adjust the path if needed

const ChatbotClient: React.FC = () => {
  return <Chatbot />;
};

export default ChatbotClient;
